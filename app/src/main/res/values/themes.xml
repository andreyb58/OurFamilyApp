<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Базовая тема -->
    <style name="Theme.Killer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorSecondaryVariant">@color/colorAccent</item>
        <item name="colorOnSecondary">@android:color/black</item>

        <!-- Status bar -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Window background -->
        <item name="android:windowBackground">@color/background</item>

        <!-- Action bar -->
        <item name="actionBarSize">56dp</item>
        <item name="actionBarStyle">@style/Widget.Killer.ActionBar</item>

        <item name="android:fontFamily">@font/gothampro</item>
    </style>

    <!-- Тема без ActionBar -->
    <style name="Theme.Killer.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Стиль для ActionBar -->
    <style name="Widget.Killer.ActionBar" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="titleTextColor">@android:color/white</item>
        <item name="subtitleTextColor">@android:color/white</item>
    </style>

    <!-- Стиль для кнопок -->
    <style name="Widget.Killer.Button" parent="Widget.MaterialComponents.Button">
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">40dp</item>
    </style>

    <!-- Стиль для карточек -->
    <style name="Widget.Killer.CardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">4dp</item>
    </style>

    <!-- Стиль для текстовых полей -->
    <style name="Widget.Killer.TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxCornerRadiusBottomEnd">20dp</item>
        <item name="boxCornerRadiusBottomStart">20dp</item>
        <item name="boxCornerRadiusTopEnd">20dp</item>
        <item name="boxCornerRadiusTopStart">20dp</item>
        <item name="boxStrokeColor">@color/colorPrimary</item>
        <item name="boxStrokeWidth">3dp</item>
        <item name="hintTextColor">@color/colorPrimary</item>
        <item name="fontFamily">@font/gothampro</item>
    </style>

    <style name="Theme.FamilyPlanner" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorSecondaryVariant">@color/colorAccent</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowBackground">@color/background_light</item>
        <item name="materialButtonStyle">@style/Widget.FamilyPlanner.Button</item>
    </style>

    <!-- Стиль для кнопок -->
    <style name="Widget.FamilyPlanner.Button" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">40dp</item>
        <item name="android:minHeight">56dp</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
    </style>

    <!-- Стиль для диалогов -->
    <style name="Theme.FamilyPlanner.Dialog" parent="ThemeOverlay.MaterialComponents.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@style/DialogAnimation</item>
    </style>
    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit</item>
    </style>

    <style name="Widget.Killer.TextInputEdit" parent="Widget.Material3.TextInputEditText.OutlinedBox">
        <item name="background">@drawable/rounded_edittext</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="hintTextColor">@color/success</item>
        <item name="fontFamily">@font/gothampro</item>
    </style>
</resources>